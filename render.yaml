databases:
  - name: c00p75_db
    databaseName: c00p75_db
    user: c00p75

services:
  - type: web
    name: CycleCruise
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: c00p75_db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false